#define BOOST_TEST_MODULE signature_provider_aws
#include <boost/test/included/unit_test.hpp>

// Boost.Test entry point for the `test_signature_provider_aws` binary: the shared AWS glue's own offline unit
// suite (`test_aws_common.cpp`). BOOST_TEST_MODULE must be defined in exactly one TU per binary.
