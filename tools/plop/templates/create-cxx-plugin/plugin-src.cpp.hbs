#include <sysio/{{name}}/{{name}}.hpp>

namespace {{namespace}} {

struct {{name}}::impl {
};

{{name}}::{{name}}()
   : _impl(std::make_unique<impl>()) {}

{{name}}::~{{name}}() = default;

void {{name}}::set_program_options(options_description& cli,
                                    options_description& cfg) {
}

void {{name}}::plugin_initialize(const variables_map& options) {
   try {

   }
   FC_LOG_AND_RETHROW()
}

void {{name}}::plugin_startup() {
   ilog("{{name}}: startup");
}

void {{name}}::plugin_shutdown() {
   ilog("{{name}}: shutdown");
}

} // namespace {{namespace}}
