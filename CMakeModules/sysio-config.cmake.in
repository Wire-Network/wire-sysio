if(NOT EOSIO_ROOT)
   list(APPEND CMAKE_MODULE_PATH @EOS_ROOT_DIR@/cmake/sysio)
else()
   # legacy behavior for anyone configuring with -DEOSIO_ROOT=/foo or such
   list(APPEND CMAKE_MODULE_PATH ${EOSIO_ROOT}/lib/cmake/sysio)
   list(APPEND CMAKE_MODULE_PATH ${EOSIO_ROOT}/lib64/cmake/sysio)
endif()
include(EosioTester)
include(EosioCheckVersion)